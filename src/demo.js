import React, { Component } from "react";
import ReactDom from "react-dom";

const Demo = (props) => {
  return <h1>Heya &!!</h1>;
};

export default Demo;

// agar class hai to {this.props} karo else {props.}
